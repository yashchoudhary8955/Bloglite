# Bloglite

Make sure you have Python and Visual Studio Code installed on your computer.

1) Clone the repository to your local machine by the folder provided:

2) Open the project in Visual Studio Code by running the following command in your terminal:
Copy code

3) Create a virtual environment to install the required packages by running the following command in your terminal:

python -m venv env

4) Activate the virtual environment by running the following command in your terminal:

source env/bin/activate

5) Install the required packages by running the following command in your terminal:

pip install -r requirements.txt

6) Run the application by running the following command in your terminal:

7) from app import db
db.create_all()

8)
export FLASK_APP=app.py
export FLASK_ENV=development

9) flask run
You can now access the application in your web browser by navigating to http://localhost:5000







